import { FiltraPorNomePipe } from './filtra-por-nome.pipe';

describe('FiltraPorNomePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraPorNomePipe();
    expect(pipe).toBeTruthy();
  });
});
